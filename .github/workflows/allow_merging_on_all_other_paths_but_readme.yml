name: Test merge behaviour
on:
  pull_request:
    paths-ignore:
      - 'README.md'

jobs:
  dummy_job:
    runs-on: ubuntu-latest
    name: Test job
    permissions:
      contents: read
    steps:
      - run: echo "No checks required."
